<form method="post" action="{{ path('app_event_payment_delete', {'id': event_payment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_payment.id) }}">
    <button class="btn">Delete</button>
</form>
