<!DOCTYPE html>

<title>EventPayment</title>

{% block body %}
    <h1>EventPayment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_payment.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_event_payment_index') }}">back to list</a>

    <a href="{{ path('app_event_payment_edit', {'id': event_payment.id}) }}">edit</a>

    {{ include('event_payment/_delete_form.html.twig') }}
{% endblock %}
