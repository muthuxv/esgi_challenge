<!DOCTYPE html>

<title>MissionType</title>

{% block body %}
    <h1>MissionType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mission_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mission_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_mission_type_index') }}">back to list</a>

    <a href="{{ path('back_mission_type_edit', {'id': mission_type.id}) }}">edit</a>

    {{ include('back/mission_type/_delete_form.html.twig') }}
{% endblock %}
