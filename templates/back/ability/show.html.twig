<!DOCTYPE html>

<title>Ability</title>

{% block body %}
    <h1>Ability</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ability.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ability.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ability.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_ability_index') }}">back to list</a>

    <a href="{{ path('back_ability_edit', {'id': ability.id}) }}">edit</a>

    {{ include('back/ability/_delete_form.html.twig') }}
{% endblock %}