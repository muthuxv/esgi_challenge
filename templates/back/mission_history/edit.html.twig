<!DOCTYPE html>

<title>Edit MissionHistory</title>

{% block body %}
    <h1>Edit MissionHistory</h1>

    {{ include('back/mission_history/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('back_mission_history_index') }}">back to list</a>

    {{ include('back/mission_history/_delete_form.html.twig') }}
{% endblock %}
